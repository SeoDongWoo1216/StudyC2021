#include <stdio.h>                // printf, scanf 함수 사용을위해 헤더파일 선언

void input_data(int *pa, int *pb) // 두 정수 입력 함수
{
	printf("두 정수 입력 : $");
	scanf("%d %d", pa, pb);
}

double average(a, b)              // 평균을 구하는 함수
{
	return (a + b) / 2;
}